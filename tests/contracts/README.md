# Contract Tests

Structured inbox-processing contract coverage currently lives in `packages/core/src/index.test.ts`.

If contract coverage outgrows the core package test surface, move the schema-boundary tests here as a dedicated workspace package.
